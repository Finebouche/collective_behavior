name: collective_env
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.11
  - ffmpeg
  - jupyterlab
  - matplotlib
  - graph-tool
  - pip
  - pip:
    - ray[rllib]
    - gymnasium
    - numpy
    - tensorflow
    #- tensorflow-metal # [osx] # doesn't seem to work to detect GPU
    - torch
    - wandb
    - ipywidgets